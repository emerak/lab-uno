.row
  .col-lg-12
    = simple_form_for @specie, url: specie_path[:url], method: specie_path[:method],  html: { class: 'form-horizontal' } do |f|
      .form-inputs
        = f.input :name
        = f.button :submit
        = link_to 'Cancel', :back
